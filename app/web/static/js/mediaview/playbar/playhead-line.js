// ─── mediaview/playbar/playhead-line.js ────────────────────────────────────
// ONE absolute-positioned vertical div, spans from the top of the
// scrubber row through the time-axis row, and every swimlane row,
// at left:<play-pct>%. Sits inside a shared content-area
// container whose left padding matches the scrubber's geometry exactly,
// so the line cuts through every row at the same x.
//
// SKELETON — task #4 builds the shared content-area container and
// mounts the line inside it.

export function mountPlayheadLine(/* contentArea */){}
