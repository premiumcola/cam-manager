{
  "name": "squirreling-sightings-frontend-tooling",
  "private": true,
  "type": "module",
  "description": "Lint + format tooling for the Squirreling · Sightings frontend. The runtime app loads JS modules directly via index.html — there's no bundler. This package only exists for the lint/format pipeline.",
  "scripts": {
    "lint": "eslint app/web/static/js",
    "lint:fix": "eslint app/web/static/js --fix",
    "format": "prettier --write 'app/web/static/**/*.{js,css,html}' 'app/web/templates/**/*.html'",
    "format:check": "prettier --check 'app/web/static/**/*.{js,css,html}' 'app/web/templates/**/*.html'"
  },
  "devDependencies": {
    "@eslint/js": "^9.15.0",
    "eslint": "^9.15.0",
    "eslint-plugin-unicorn": "^56.0.0",
    "prettier": "^3.4.0"
  }
}
